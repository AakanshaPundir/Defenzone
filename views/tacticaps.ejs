<!DOCTYPE html>
<html>
<head>
    <title><%= title %></title>
    <link href="https://cdn.jsdelivr.net/npm/bootstrap@5.3.2/dist/css/bootstrap.min.css" rel="stylesheet">
    <link rel="stylesheet" href="/css/style.css">
</head>
<body>
    <%- include('partials/header') %>


    <div class="container my-5">
        <% tacticaps.forEach(tacticap => { %>
            <div class="row mb-5 align-items-center border-bottom pb-4">
                <div class="col-md-4 text-center">
                    <img src="<%= tacticap.image %>" alt="<%= tacticap.name %>" class="img-fluid">
                </div>
                <div class="col-md-8">
                    <h3><%= tacticap.name %></h3>
                    <p class="text-muted">Regular, Armed forces special, Tacticap</p>
                    <h4 class="text-danger"><%= tacticap.price %></h4>
                    <ul>
                        <li><strong>Color:</strong> <%= tacticap.colors.join('/') %></li>
                        <li><strong>Design:</strong> <%= tacticap.design %></li>
                        <li><strong>Style:</strong> <%= tacticap.style %></li>
                        <li><strong>Vibe:</strong> <%= tacticap.vibe %></li>
                        <li><strong>Perfect For:</strong> <%= tacticap.perfectFor %></li>
                    </ul>
                    <button class="btn btn-dark">SELECT OPTIONS</button>
                </div>
            </div>
        <% }) %>
    </div>

    <%- include('partials/footer') %>
    <script src="https://cdn.jsdelivr.net/npm/bootstrap@5.3.2/dist/js/bootstrap.bundle.min.js"></script>
</body>
</html>
