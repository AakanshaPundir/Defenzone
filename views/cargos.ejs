<!DOCTYPE html>
<html>
<head>
    <title><%= title %></title>
    <link href="https://cdn.jsdelivr.net/npm/bootstrap@5.3.2/dist/css/bootstrap.min.css" rel="stylesheet">
    <link rel="stylesheet" href="/css/style.css">
</head>
<body>
    <%- include('partials/header') %>


    <div class="container my-5">
        <% cargos.forEach(cargo => { %>
            <div class="row mb-5 align-items-center border-bottom pb-4">
                <div class="col-md-4 text-center">
                    <img src="<%= cargo.image %>" alt="<%= cargo.name %>" class="img-fluid">
                </div>
                <div class="col-md-8">
                    <h3><%= cargo.name %></h3>
                    <p class="text-muted">Regular, Armed forces special, Cargo</p>
                    <h4 class="text-danger"><%= cargo.price %></h4>
                    <ul>
                        <li><strong>Color:</strong> <%= cargo.colors.join('/') %></li>
                        <li><strong>Design:</strong> <%= cargo.design %></li>
                        <li><strong>Style:</strong> <%= cargo.style %></li>
                        <li><strong>Vibe:</strong> <%= cargo.vibe %></li>
                        <li><strong>Perfect For:</strong> <%= cargo.perfectFor %></li>
                    </ul>
                    <button class="btn btn-dark">SELECT OPTIONS</button>
                </div>
            </div>
        <% }) %>
    </div>

    <%- include('partials/footer') %>
    <script src="https://cdn.jsdelivr.net/npm/bootstrap@5.3.2/dist/js/bootstrap.bundle.min.js"></script>
</body>
</html>
